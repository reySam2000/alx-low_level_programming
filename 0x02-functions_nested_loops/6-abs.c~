#include "main.h"

/**
 * abs - computes an integer's absolute value 
 * @n: check this integer
 * Return: the integer's absolute value 
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}

